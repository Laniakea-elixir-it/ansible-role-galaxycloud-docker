---
- hosts: vmtest
  remote_user: root
  become: true
  vars:
    galaxy_flavor: 'bgruening/galaxy-stable:20.05'
  roles:
    - ../../ansible-role-laniakea-galaxy-docker
  pre_tasks:
    
    - name: install pip
      apt:
        name: python3-pip
        update_cache: yes
      when: ansible_facts['distribution'] == "Ubuntu"
#
#    - name: install pip
#      yum:
#        name: python3-pip
#      when: ansible_facts['distribution'] == "CentOS"
